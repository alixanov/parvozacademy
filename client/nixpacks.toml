# Nixpacks config for FRONTEND-ONLY Railway service
# Root directory must be set to client/ in the Railway service settings.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
